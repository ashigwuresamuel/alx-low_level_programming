#include "main.h"

/**
 * add - adds two integers and returns the result
 * @e: int to be added to b
 * @f: int to be added to a
 * Return: sum of e and f
 */
int add(int e, int f)
{
	int g;

	g = e + f;

	return (g);
}
